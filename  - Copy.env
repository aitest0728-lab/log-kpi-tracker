# --- Tableau login (used by fetch_tableau_reports / Playwright) ---
TABLEAU_URL=https://inhouse-analytics.hktv.com.hk/#/signin
TABLEAU_USER=
TABLEAU_PASS=

# --- Local folders the script reads/writes ---
OIX_FOLDER=C:\Users\chipanl\Downloads\Digimobi Report
REPORT_FOLDER=C:\Users\chipanl\Downloads\Whatsapp Session\log-kpi-tracker\Folder for KPI Dashboard

# Point this at the copy of data.json inside your cloned GitHub repo (inside
# the public/ folder, since that's what the Cloudflare Worker serves) so
# committing the repo picks up the new numbers automatically.
DATA_JSON_PATH=./public/data.json
HISTORY_PATH=./history.json
